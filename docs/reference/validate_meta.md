# Validate a metadata table

`meta` must be a `data.frame` with `sample` as the first column and one
row per sample.

## Usage

``` r
validate_meta(meta)
```

## Arguments

- meta:

  A sample metadata table.

## Value

`TRUE` invisibly on success.
