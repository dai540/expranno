# Example expression and metadata inputs

Returns a small human RNA-seq example that follows the package input
contract. The toy matrix is suitable for examples, tests, and
documentation.

## Usage

``` r
example_expranno_data()
```

## Value

A named list with `expr` and `meta`.
