% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{annotate_expr}
\alias{annotate_expr}
\title{Annotate an expression matrix}
\usage{
annotate_expr(
  expr,
  meta = NULL,
  species = c("auto", "human", "mouse"),
  annotation_engine = c("hybrid", "biomart", "orgdb", "ensdb", "none"),
  fields = NULL,
  strip_version = TRUE,
  biomart_version = 102,
  biomart_host = NULL,
  biomart_mirror = NULL,
  assay_name = NULL,
  gene_id_col = NULL,
  sample_col = "sample",
  output_file = NULL,
  report_file = NULL,
  ambiguity_file = NULL,
  provenance_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{expr}{Expression table with \code{gene_id} in the first column, or a
\code{SummarizedExperiment}-like object.}

\item{meta}{Metadata table with \code{sample} in the first column. Leave \code{NULL}
when \code{expr} is a \code{SummarizedExperiment} and metadata should be taken from
\code{colData}.}

\item{species}{Either \code{"auto"}, \code{"human"}, or \code{"mouse"}.}

\item{annotation_engine}{Annotation backend strategy. Use \code{"hybrid"} to try
\code{biomaRt}, \code{orgdb}, and \code{EnsDb} in sequence.}

\item{fields}{Optional annotation fields to keep.}

\item{strip_version}{Whether to remove Ensembl version suffixes.}

\item{biomart_version}{Fixed Ensembl release used by the \code{biomaRt} backend.}

\item{biomart_host}{Optional explicit Ensembl host.}

\item{biomart_mirror}{Optional Ensembl mirror name.}

\item{assay_name}{Optional assay name when \code{expr} is a
\code{SummarizedExperiment}.}

\item{gene_id_col}{Optional row-data column containing Ensembl IDs when
\code{expr} is a \code{SummarizedExperiment}.}

\item{sample_col}{Metadata column to use as \code{sample} when \code{expr} is a
\code{SummarizedExperiment}.}

\item{output_file}{Optional CSV path for the annotated matrix.}

\item{report_file}{Optional CSV path for the coverage report.}

\item{ambiguity_file}{Optional CSV path for the ambiguity report.}

\item{provenance_file}{Optional CSV path for backend provenance.}

\item{verbose}{Whether to emit progress messages.}
}
\value{
An \code{expranno_annotation} object.
}
\description{
Validates input, normalizes Ensembl IDs, infers or accepts the species, runs
a coverage-first annotation workflow, binds annotation columns to the
expression table, and optionally writes \code{expr_anno.csv} plus provenance and
ambiguity reports.
}
