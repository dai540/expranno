% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_annotation_engines}
\alias{validate_annotation_engines}
\title{Validate annotation engines against a truth table}
\usage{
validate_annotation_engines(
  expr,
  meta = NULL,
  truth,
  truth_gene_col = "gene_id",
  species = c("auto", "human", "mouse"),
  annotation_preset = NULL,
  engines = c("biomart", "orgdb", "ensdb", "hybrid"),
  fields = NULL,
  strip_version = TRUE,
  biomart_version = 102,
  biomart_host = NULL,
  biomart_mirror = NULL,
  assay_name = NULL,
  gene_id_col = NULL,
  sample_col = "sample",
  output_file = NULL,
  detail_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{expr}{Expression table with \code{gene_id} first, or a
\code{SummarizedExperiment}-like object.}

\item{meta}{Metadata table with \code{sample} first. Leave \code{NULL} when \code{expr} is a
\code{SummarizedExperiment}.}

\item{truth}{A data frame containing a \code{gene_id} column and one or more truth
annotation fields such as \code{symbol} or \code{gene_name}.}

\item{truth_gene_col}{Column in \code{truth} containing the Ensembl gene ID.}

\item{species}{Either \code{"auto"}, \code{"human"}, or \code{"mouse"}.}

\item{annotation_preset}{Optional preset that fixes a reproducible
annotation configuration. Supported values are \code{"human_v102"},
\code{"mouse_v102"}, \code{"human_tpm_v102"}, \code{"mouse_tpm_v102"},
\code{"human_count_v102"}, and \code{"mouse_count_v102"}.}

\item{engines}{Character vector of annotation engines to compare.}

\item{fields}{Optional annotation fields to validate. Defaults to the
intersection between \code{truth} and the package's standard annotation fields.}

\item{strip_version}{Whether to remove Ensembl version suffixes in both
prediction and truth tables.}

\item{biomart_version}{Fixed Ensembl release used by the \code{biomaRt} backend.}

\item{biomart_host}{Optional explicit Ensembl host.}

\item{biomart_mirror}{Optional Ensembl mirror name.}

\item{assay_name}{Optional assay name when \code{expr} is a
\code{SummarizedExperiment}.}

\item{gene_id_col}{Optional row-data column containing Ensembl IDs when
\code{expr} is a \code{SummarizedExperiment}.}

\item{sample_col}{Metadata column to use as \code{sample} when \code{expr} is a
\code{SummarizedExperiment}.}

\item{output_file}{Optional CSV path for the validation summary.}

\item{detail_file}{Optional CSV path for per-gene validation details.}

\item{verbose}{Whether to emit progress messages.}
}
\value{
An \code{expranno_validation} object.
}
\description{
Runs one or more annotation engines, then compares the chosen annotation
fields against a user-supplied truth table keyed by Ensembl gene ID.
}
