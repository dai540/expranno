% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_annotation_engines}
\alias{benchmark_annotation_engines}
\title{Benchmark annotation engines on the same input}
\usage{
benchmark_annotation_engines(
  expr,
  meta = NULL,
  species = c("auto", "human", "mouse"),
  engines = c("none", "biomart", "orgdb", "ensdb", "hybrid"),
  fields = NULL,
  strip_version = TRUE,
  biomart_version = 102,
  biomart_host = NULL,
  biomart_mirror = NULL,
  assay_name = NULL,
  gene_id_col = NULL,
  sample_col = "sample",
  output_file = NULL,
  coverage_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{expr}{Expression table with \code{gene_id} first, or a
\code{SummarizedExperiment}-like object.}

\item{meta}{Metadata table with \code{sample} first. Leave \code{NULL} when \code{expr} is a
\code{SummarizedExperiment}.}

\item{species}{Either \code{"auto"}, \code{"human"}, or \code{"mouse"}.}

\item{engines}{Character vector of annotation engines to compare.}

\item{fields}{Optional annotation fields to benchmark.}

\item{strip_version}{Whether to remove Ensembl version suffixes.}

\item{biomart_version}{Fixed Ensembl release used by the \code{biomaRt} backend.}

\item{biomart_host}{Optional explicit Ensembl host.}

\item{biomart_mirror}{Optional Ensembl mirror name.}

\item{assay_name}{Optional assay name when \code{expr} is a
\code{SummarizedExperiment}.}

\item{gene_id_col}{Optional row-data column containing Ensembl IDs when
\code{expr} is a \code{SummarizedExperiment}.}

\item{sample_col}{Metadata column to use as \code{sample} when \code{expr} is a
\code{SummarizedExperiment}.}

\item{output_file}{Optional CSV path for the benchmark summary.}

\item{coverage_file}{Optional CSV path for long-format coverage results.}

\item{verbose}{Whether to emit progress messages.}
}
\value{
An \code{expranno_benchmark} object.
}
\description{
Runs one or more annotation engines on the same expression and metadata
inputs, then summarizes coverage, ambiguity, and provenance for comparison.
}
